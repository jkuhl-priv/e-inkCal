# template for a config file
# calDAV url
server
# username
user
# password
password
# calendar(s) that are treated as birthday calendars
# birthdays 
# savefile on device in case of connection loss
datafile ../mydata.p
# if you don't want to load all calendars, select a few with this command, seperate the calendar names by ";"
calendars
# select the weekday format, valid choices are "FULL" ""
weekday_format 
# select if the date is drawn
draw_date True
# if you have a e-ink display that can show color, you can use this command to choose if color is used
colormode False
# set this ony to true in production, for testing the images produced are just put in the script folder
on_e_paper False